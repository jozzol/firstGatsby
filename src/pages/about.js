import * as React from 'react';
import Layout from '../components/layout';

const About = () =>{
    return(
        <Layout title='About us' heading='About'>
            <p>simple page with gatsby using its core features and graphql</p>
        </Layout>
    )
};

export default About;