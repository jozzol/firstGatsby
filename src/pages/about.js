import * as React from 'react';
import Layout from '../components/layout';

const About = () =>{
    return(
        <Layout title='About us' heading='About'>
            <p>First gatsby page using the tutorial and experimenting</p>
        </Layout>
    )
};

export default About;